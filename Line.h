#pragma once
#include <string>
#include <iostream>
using namespace std;

void FillLine(char ch, int length, bool change = true);

void CenterLetter(char ch, int length, string letter);

char Check(bool mark = false);